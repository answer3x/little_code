一个序列，全是1和0，求最长的子序列，该子序列中1的个数和0的个数相等

分析：
把0替换成-1，如果这一段 0 1 个数相等，那么这一段的和为0
然后算出sum[0] = a[0];
sum[i] = sum[i-1] + a[i] （0 < i < n） 
找出相等的max(j-i) 满足条件 sum[i]  = sum[j]

不过复杂度是nlogn  或许还有n复杂度的

因为 sum(i) 出现的所有可能的值是连续的，就是说任何 | sum(i) - sum(i+1) | = 1 
所以sum的值在一个有限的范围内, 那么可以 O(n) 查找到最大长度

